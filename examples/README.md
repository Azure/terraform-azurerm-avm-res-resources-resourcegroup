# Examples

This directory contains runnable examples for this module:

- `default`: Deploy a resource group with the minimum required values.
- `complete`: Deploy with lock, role assignments, and tags.
- `managed_by`: Deploy a resource group configured with `managed_by`.
- `ignore_tag_changes`: Deploy with `ignore_tag_changes = true` to ignore tag drift.