# Managed By example

This example demonstrates how to set the `managed_by` attribute on a resource group. The `managed_by` value indicates that the resource group is managed by another service (for example a managed application or a Databricks workspace) and is surfaced as the `managedBy` property on the underlying ARM resource.
